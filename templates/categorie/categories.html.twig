{% extends 'DashbordBack.html.twig' %}

{% block title %}{% endblock %}

{% block content %}

    <div class="card">
        <div class="card-header">
            <h3 class="card-title">LISTE DES CATEGORIES : /</h3>
        </div>
        <div class="card-body p-0">
            <form class="w-search">
                <div class="sidebar-search">

                    <div class="input-group custom-search-form">

                        <input type="text" id="search" class="form-control pl-3" placeholder="Search here">
                    </div>
                    <!-- /input-group -->
                </div>
                <ul class="nav" id="side-menu">
                    <li>
                        <a href="#"><span class="fa arrow"></span></a>
                        <ul style="background-color: #0b0b0b" class="nav " id="entitiesNav">
                        </ul>
                    </li>
                </ul>



            </form>
            <br>
            <br>
            <br>
            <table id="cat" class="table table-striped">
                <thead>
                <tr>
                    <th style="width: 10px">#</th>
                    <th>nom du categorie</th>
                    <th>Action</th>

                </tr>
                </thead>
                <tbody>
                {% for c in categorie %}
                    <tr>
                        <td>{{ c.id }}</td>
                        <td>{{ c.nomCat }}</td>


                        <td>
                            <a class="btn btn-block btn-success"  href="{{ path('edit_categorie',{'idCategorie':c.id}) }}" > modifier</a>
                        </td>
                        <td>
                            <a class="btn btn-block btn-danger"   onclick="return confirm('Etes-vous sûr de supprimer cet categorie?');"href="{{ path('delete_categorie',{'idCategorie':c.id}) }}" >supprimer </a>
                        </td>

                        </td>
                    </tr>


                {% endfor %}
                </tbody>

            </table>
        </div>



    </div>
{% endblock content %}
